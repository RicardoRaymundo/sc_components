import 'package:flutter/material.dart';

class OccurrenceLocalWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("textFinder"),
    );
  }
}
